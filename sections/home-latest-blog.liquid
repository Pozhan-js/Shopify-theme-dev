<div class="home-latest-blog" style="padding: 48px 0;">
  <h2 style="color: #D8A26C; font-weight: bold; font-size: 2rem; text-align: center; margin-bottom: 32px;">最新烘焙教程</h2>
  <div style="display: flex; gap: 32px; justify-content: center; flex-wrap: wrap;">
    {% assign latest_blog = blogs['News'] %}
    {% if latest_blog.articles.size > 0 %}
      {% for article in latest_blog.articles limit:3 %}
        <div style="width: 320px; background: #fff; border-radius: 12px; box-shadow: 0 2px 8px #eee; padding: 24px; display: flex; flex-direction: column;">
          <a href="{{ article.url }}">
            <img src="{{ article.image | img_url: '400x220' }}" alt="{{ article.title }}" style="width: 100%; height: 160px; object-fit: cover; border-radius: 8px; margin-bottom: 16px;">
          </a>
          <div style="font-size: 1.1rem; font-weight: bold; color: #222; margin-bottom: 8px;">{{ article.title }}</div>
          <div style="color: #666; font-size: 0.95rem; margin-bottom: 16px;">{{ article.excerpt | strip_html | truncate: 60 }}</div>
          <a href="{{ article.url }}" style="color: #D8A26C; font-weight: bold; text-decoration: underline;">阅读更多</a>
        </div>
      {% endfor %}
    {% else %}
      <div style="text-align:center; color:#999; width:100%;">暂无教程文章，请在后台添加。</div>
    {% endif %}
  </div>
</div>