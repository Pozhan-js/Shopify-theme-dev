<div class="hero-banner">
  <div class="hero-content">
    <div class="container">
      <div class="hero-text">
        <h1 class="hero-title">
          {{ section.settings.title | default: "Your Trusted Partner for Pilates Equipment Manufacturing" }}
        </h1>
        <p class="hero-description">
          {{ section.settings.description | default: "At XS Pilates, we specialize in OEM services for premium Pilates equipment and accessories. With 20+ years of expertise and a world-class factory, we help global brands and studios bring their ideas to life." }}
        </p>
        {% if section.settings.button_text != blank %}
          <div class="hero-cta">
            <a href="{{ section.settings.button_url | default: '#' }}" class="btn btn-primary">
              {{ section.settings.button_text }}
            </a>
          </div>
        {% endif %}
      </div>
    </div>
  </div>
  <div class="hero-background">
    {% if section.settings.background_image != blank %}
      <img src="{{ section.settings.background_image | img_url: '1920x1080' }}" alt="{{ section.settings.title }}" class="hero-bg-image">
    {% else %}
      <div class="hero-bg-placeholder"></div>
    {% endif %}
    <div class="hero-overlay"></div>
  </div>
</div>

<style>
.hero-banner {
  position: relative;
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  border-radius: 16px;
}

.hero-background {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 1;
}

.hero-bg-image {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
}

.hero-bg-placeholder {
  width: 100%;
  height: 100%;
  background: linear-gradient(135deg, #8B7355 0%, #A0926B 50%, #8B7355 100%);
}

.hero-overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.4);
  z-index: 2;
}

.hero-content {
  position: relative;
  z-index: 3;
  width: 100%;
  text-align: center;
  color: white;
}

.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 20px;
}

.hero-text {
  max-width: 1081px;
  margin: 0 auto;
}

.hero-title {
  font-size: 48px;
  font-weight: 500;
  line-height: 1.2;
  margin-bottom: 28px;
  color: white;
  text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
  padding: 0px 86px;
}

.hero-description {
  font-size: 18px;
  font-weight: 400;
  line-height: 36px;
  margin-bottom: 2.5rem;
  color: white;
  opacity: 0.95;
  text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
  max-width: 1081px;
  margin-left: auto;
  margin-right: auto;
}

.hero-cta {
  margin-top: 2rem;
}

.btn {
  display: inline-block;
  padding: 15px 30px;
  font-size: 1.1rem;
  font-weight: 600;
  text-decoration: none;
  border-radius: 5px;
  transition: all 0.3s ease;
  cursor: pointer;
  border: none;
}

.btn-primary {
  background-color: #D4AF37;
  color: white;
  box-shadow: 0 4px 15px rgba(212, 175, 55, 0.3);
}

.btn-primary:hover {
  background-color: #B8941F;
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(212, 175, 55, 0.4);
}

/* 响应式设计 */
/* 大屏幕 (1920px+) */
@media (min-width: 1920px) {
  .hero-title {
    font-size: 48px;
    line-height: 60px;
  }
  
  .hero-description {
    font-size: 18px;
    line-height: 36px;
    max-width: 1081px;
  }
}

/* 笔记本电脑 (1366px - 1919px) */
@media (max-width: 1919px) and (min-width: 1366px) {
  .hero-title {
    font-size: 42px;
    line-height: 52px;
  }
  
  .hero-description {
    font-size: 16px;
    line-height: 32px;
    max-width: 950px;
  }
  
  .container {
    max-width: 1100px;
  }
}

/* 小笔记本 (1024px - 1365px) */
@media (max-width: 1365px) and (min-width: 1024px) {
  .hero-title {
    font-size: 38px;
    line-height: 48px;
  }
  
  .hero-description {
    font-size: 15px;
    line-height: 30px;
    max-width: 800px;
  }
  
  .container {
    max-width: 960px;
    padding: 0 30px;
  }
}

/* 平板横屏 (768px - 1023px) */
@media (max-width: 1023px) and (min-width: 768px) {
  .hero-banner {
    min-height: 85vh;
  }
  
  .hero-title {
    font-size: 34px;
    line-height: 44px;
    margin-bottom: 24px;
  }
  
  .hero-description {
    font-size: 14px;
    line-height: 28px;
    max-width: 700px;
    margin-bottom: 2rem;
  }
  
  .container {
    padding: 0 25px;
  }
  
  .btn {
    padding: 14px 28px;
    font-size: 1.05rem;
  }
}

/* 平板竖屏和大手机 (481px - 767px) */
@media (max-width: 767px) and (min-width: 481px) {
  .hero-banner {
    min-height: 80vh;
  }
  
  .hero-title {
    font-size: 30px;
    line-height: 38px;
    margin-bottom: 20px;
  }
  
  .hero-description {
    font-size: 13px;
    line-height: 26px;
    margin-bottom: 1.8rem;
  }
  
  .container {
    padding: 0 20px;
  }
  
  .btn {
    padding: 13px 26px;
    font-size: 1rem;
  }
}

/* 小手机 (320px - 480px) */
@media (max-width: 480px) {
  .hero-banner {
    min-height: 75vh;
  }
  
  .hero-title {
    font-size: 26px;
    line-height: 34px;
    margin-bottom: 18px;
  }
  
  .hero-description {
    font-size: 12px;
    line-height: 24px;
    margin-bottom: 1.5rem;
  }
  
  .container {
    padding: 0 15px;
  }
  
  .btn {
    padding: 12px 24px;
    font-size: 0.95rem;
  }
}

/* 超小屏幕 (最小320px) */
@media (max-width: 320px) {
  .hero-title {
    font-size: 24px;
    line-height: 32px;
  }
  
  .hero-description {
    font-size: 11px;
    line-height: 22px;
  }
  
  .btn {
    padding: 10px 20px;
    font-size: 0.9rem;
  }
}
</style>

{% schema %}
{
  "name": "Hero Banner",
  "tag": "section",
  "class": "hero-banner-section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Banner Title",
      "default": "Your Trusted Partner for Pilates Equipment Manufacturing"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Banner Description",
      "default": "At XS Pilates, we specialize in OEM services for premium Pilates equipment and accessories. With 20+ years of expertise and a world-class factory, we help global brands and studios bring their ideas to life."
    },
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Background Image"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text"
    },
    {
      "type": "url",
      "id": "button_url",
      "label": "Button URL"
    },
    {
      "type": "header",
      "content": "Style Settings"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "label": "Overlay Opacity",
      "min": 0,
      "max": 100,
      "step": 10,
      "default": 40,
      "unit": "%"
    }
  ],
  "presets": [
    {
      "name": "Hero Banner",
      "settings": {
        "title": "Your Trusted Partner for Pilates Equipment Manufacturing",
        "description": "At XS Pilates, we specialize in OEM services for premium Pilates equipment and accessories. With 20+ years of expertise and a world-class factory, we help global brands and studios bring their ideas to life."
      }
    }
  ]
}
{% endschema %}
