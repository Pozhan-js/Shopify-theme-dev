<style>
  /* Header Navigation Styles */
  .nav-blur {
    backdrop-filter: blur(12px);
    background: #5A5A5A;
    border: 1px solid rgba(255, 255, 255, 0.15);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
    border-radius: 0;
    margin: 0;
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 50;
  }

  .nav-container {
    padding: 8px 16px;
    min-height: calc(32px + 16px);
    display: flex;
    align-items: center;
  }

  .nav-flex {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 100%;
    width: 100%;
  }

  .logo-container {
    display: flex;
    align-items: center;
    gap: 12px;
    height: 100%;
  }

  .logo-bg {
    background: #FFFFFF;
    color: #DC2626;
    width: 32px;
    height: calc(32px + 8px);
    border-radius: 6px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    font-size: 16px;
  }

  .logo-image {
    height: calc(32px + 8px);
    border-radius: 6px;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .logo-image img {
    height: 100%;
    width: auto;
    border-radius: 6px;
  }

  .logo-container {
    display: flex;
    align-items: center;
  }

  .desktop-nav {
    display: none;
    align-items: center;
    gap: 24px;
    margin-left: 32px;
  }

  .dropdown {
    position: relative;
    white-space: nowrap;
  }

  .nav-item {
    color: #FFFFFF;
    font-weight: 500;
    font-size: 13px;
    letter-spacing: 0.01em;
    transition: all 0.2s ease;
    text-decoration: none;
    display: flex;
    align-items: center;
    padding: 8px 10px;
    border-radius: 6px;
    min-width: fit-content;
    height: 32px;
  }

  .nav-item:hover {
    color: #E5E7EB;
  }

  .dropdown-arrow::after {
    content: '';
    display: inline-block;
    width: 8px;
    height: 8px;
    margin-left: 6px;
    border-right: 1.5px solid currentColor;
    border-bottom: 1.5px solid currentColor;
    transform: rotate(45deg) translateY(-1px);
    transition: transform 0.2s ease;
    vertical-align: baseline;
  }

  .dropdown:hover .dropdown-arrow::after {
    transform: rotate(225deg) translateY(-1px);
  }

  .dropdown-menu {
    background: rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(16px);
    border: 1px solid rgba(255, 255, 255, 0.12);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
    position: absolute;
    top: 100%;
    left: 0;
    margin-top: 8px;
    width: 192px;
    border-radius: 8px;
    opacity: 0;
    visibility: hidden;
    transition: all 0.2s ease;
  }

  .dropdown:hover .dropdown-menu {
    opacity: 1;
    visibility: visible;
  }

  .dropdown-item {
    display: block;
    padding: 8px 16px;
    font-size: 14px;
    color: #FFFFFF;
    text-decoration: none;
    transition: background-color 0.2s ease;
  }

  .dropdown-item:hover {
    background: rgba(255, 255, 255, 0.1);
    color: #FFFFFF;
  }

  .right-actions {
    display: flex;
    align-items: center;
    gap: 16px;
  }

  .contact-badge {
    background: none;
    border: none;
    padding: 0;
    display: none;
    align-items: center;
    gap: 8px;
    color: #FFFFFF;
    font-size: 14px;
    font-weight: 500;
    transition: all 0.3s ease;
    cursor: pointer;
    text-decoration: none;
    height: 32px;
  }

  .contact-badge:hover {
    color: #E5E7EB;
  }

  .contact-arrow-icon {
    background: #B33C3B;
    width: 20px;
    height: 20px;
    border-radius: 4px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
  }

  .contact-badge:hover .contact-arrow-icon {
    background: #9A2F2E;
    transform: translateX(2px) translateY(-2px);
  }

  .search-icon {
    transition: all 0.3s ease;
    color: #FFFFFF;
    background: none;
    border: none;
    padding: 8px;
    cursor: pointer;
    border-radius: 4px;
    height: 32px;
    width: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .search-icon:hover {
    transform: scale(1.1);
    color: #DC2626;
  }

  .mobile-menu-toggle {
    background: none;
    border: none;
    color: #FFFFFF;
    padding: 8px;
    cursor: pointer;
    display: flex;
    height: 32px;
    width: 32px;
    align-items: center;
    justify-content: center;
  }

  .mobile-menu {
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    background: #5A5A5A;
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-top: none;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
    display: none;
    padding: 16px;
  }

  .mobile-menu-content {
    display: flex;
    flex-direction: column;
    gap: 12px;
    width: 100%;
  }

  .mobile-menu-item {
    width: 100%;
    text-align: left;
    color: #FFFFFF;
    background: none;
    border: none;
    padding: 12px 0;
    font-size: 16px;
    font-weight: 500;
    cursor: pointer;
    transition: color 0.2s ease;
    text-decoration: none;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .mobile-menu-item:last-child {
    border-bottom: none;
  }

  .mobile-menu-item:hover {
    color: #E5E7EB;
  }

  .mobile-submenu {
    margin-left: 0;
    margin-top: 8px;
    padding-left: 16px;
    display: flex;
    flex-direction: column;
    gap: 8px;
    border-left: 2px solid rgba(255, 255, 255, 0.2);
  }

  .mobile-submenu-item {
    color: #9CA3AF;
    text-decoration: none;
    padding: 8px 0;
    font-size: 14px;
    transition: color 0.2s ease;
  }

  .mobile-submenu-item:hover {
    color: #FFFFFF;
  }

  .mobile-contact-badge {
    background: none;
    border: none;
    padding: 16px 0;
    display: flex;
    align-items: center;
    justify-content: flex-start;
    gap: 8px;
    color: #FFFFFF;
    font-size: 16px;
    font-weight: 500;
    margin-top: 16px;
    cursor: pointer;
    text-decoration: none;
    transition: all 0.3s ease;
    border-top: 1px solid rgba(255, 255, 255, 0.2);
  }

  .mobile-contact-badge:hover {
    color: #E5E7EB;
  }

  .mobile-contact-badge .contact-arrow-icon {
    width: 18px;
    height: 18px;
  }

  /* Responsive styles */
  @media (min-width: 1024px) {
    .nav-blur {
      border-radius: 9999px;
      margin: 24px 32px;
    }

    .nav-container {
      padding: 8px 32px;
      min-height: calc({{ section.settings.logo_size | default: 48 }}px + 16px);
    }

    .logo-bg {
      width: {{ section.settings.logo_size | default: 48 }}px;
      height: calc({{ section.settings.logo_size | default: 48 }}px + 8px);
      font-size: {{ section.settings.logo_size | default: 48 | times: 0.5 }}px;
    }

    .logo-image {
      height: calc({{ section.settings.logo_size | default: 48 }}px + 8px);
    }

    .nav-item {
      height: {{ section.settings.logo_size | default: 48 }}px;
    }

    .contact-badge {
      display: flex;
      height: {{ section.settings.logo_size | default: 48 }}px;
    }

    .contact-arrow-icon {
      width: 24px;
      height: 24px;
    }

    .search-icon {
      height: {{ section.settings.logo_size | default: 48 }}px;
      width: {{ section.settings.logo_size | default: 48 }}px;
    }

    .mobile-menu-toggle {
      display: none;
      height: {{ section.settings.logo_size | default: 48 }}px;
      width: {{ section.settings.logo_size | default: 48 }}px;
    }

    .desktop-nav {
      display: flex;
      flex: 1;
      justify-content: flex-start;
    }

    .mobile-menu {
      display: none !important;
    }
  }

  @media (min-width: 1280px) {
    .desktop-nav {
      gap: 32px;
    }
  }
</style>

<header>
  <div class="nav-blur">
    <nav class="nav-container">
      <div class="nav-flex">
        <!-- Logo -->
        <div class="logo-container">
          <a href="{{ routes.root_url }}">
            {% if section.settings.logo_image %}
              <div class="logo-image">
                {% assign logo_size_2x = section.settings.logo_size | default: 48 | times: 2 %}
                <img
                  src="{{ section.settings.logo_image | img_url: logo_size_2x }}"
                  alt="{{ section.settings.site_name | default: shop.name }}"
                  width="{{ section.settings.logo_size | default: 48 }}"
                  height="{{ section.settings.logo_size | default: 48 }}">
              </div>
            {% else %}
              <div class="logo-bg">
                <span>{{ section.settings.logo_text | default: 'XS' }}</span>
              </div>
            {% endif %}
          </a>
        </div>

        <!-- Desktop Navigation -->
        <div class="desktop-nav">
          {% assign main_menu_handle = section.settings.main_menu | default: 'main-menu' %}
          {% assign main_menu = linklists[main_menu_handle] %}
          {% if main_menu and main_menu.links.size > 0 %}
            {% for link in main_menu.links %}
              <div class="dropdown">
                {% if link.links.size > 0 %}
                  <span class="nav-item" style="cursor: pointer;">
                    <span>{{ link.title | escape }}</span>
                    <span class="dropdown-arrow"></span>
                  </span>
                  <div class="dropdown-menu">
                    <div style="padding: 8px 0;">
                      {% for child_link in link.links %}
                        <a href="{{ child_link.url }}" class="dropdown-item">
                          {{ child_link.title | escape }}
                        </a>
                      {% endfor %}
                    </div>
                  </div>
                {% else %}
                  <a href="{{ link.url }}" class="nav-item">
                    <span>{{ link.title | escape }}</span>
                  </a>
                {% endif %}
              </div>
            {% endfor %}
          {% endif %}
        </div>

        <!-- Right Side Actions -->
        <div class="right-actions">
          <!-- Contact Us Button -->
          <a href="{{ section.settings.contact_link }}" class="contact-badge">
            <span>{{ section.settings.contact_text | default: 'Contact Us' | escape }}</span>
            <div class="contact-arrow-icon">
              <svg
                viewBox="0 0 10.417 10.3762"
                xmlns="http://www.w3.org/2000/svg"
                xmlns:xlink="http://www.w3.org/1999/xlink"
                width="10.416992"
                height="10.376221"
                fill="none"
                customFrame="#000000">
                <g id="组合 24">
                <g id="组合 3">
                  <path
                    id="多边形 7"
                    d="M-0.353553 3.26282L3.61638 -0.707107L7.58631 3.26282L6.8792 3.96993L3.26282 0.353553L3.61638 0L3.96993 0.353553L0.353553 3.96993L-0.353553 3.26282Z"
                    fill="rgb(255,255,255)"
                    fill-rule="nonzero"
                    transform="matrix(0.708494,0.705717,-0.708494,0.705716,5.29272,0)" />
                  <line
                    id="直线 2"
                    x1="0"
                    x2="7.23275518"
                    y1="0"
                    y2="0"
                    stroke="rgb(255,255,255)"
                    stroke-width="1"
                    transform="matrix(-0.708494,0.705716,-0.708494,-0.705717,7.73047,2.76355)" />
                </g>
              </g>
            </svg>

          </div>
        </a>

        <!-- Search Icon -->
        <button class="search-icon" onclick="toggleSearch()">
          <svg
            width="20"
            height="20"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24">
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
          </svg>
        </button>

        <!-- Mobile Menu Button -->
        <button class="mobile-menu-toggle" onclick="toggleMobileMenu()">
          <svg
            class="menu-icon"
            width="24"
            height="24"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24">
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M4 6h16M4 12h16M4 18h16" />
          </svg>
          <svg
            class="close-icon"
            width="24"
            height="24"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            style="display: none;">
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
    </div>

    <!-- Mobile Menu -->
    <div class="mobile-menu" id="mobileMenu">
      <div class="mobile-menu-content">
        {% if main_menu and main_menu.links.size > 0 %}
          {% for link in main_menu.links %}
            <div>
              {% if link.links.size > 0 %}
                <button class="mobile-menu-item" onclick="toggleSubmenu('submenu{{ forloop.index }}')">
                  {{ link.title | escape }}
                </button>
                <div
                  class="mobile-submenu"
                  id="submenu{{ forloop.index }}"
                  style="display: none;">
                  {% for child_link in link.links %}
                    <a href="{{ child_link.url }}" class="mobile-submenu-item">
                      {{ child_link.title | escape }}
                    </a>
                  {% endfor %}
                </div>
              {% else %}
                <a href="{{ link.url }}" class="mobile-menu-item">
                  {{ link.title | escape }}
                </a>
              {% endif %}
            </div>
          {% endfor %}
        {% endif %}

        <a href="{{ section.settings.contact_link }}" class="mobile-contact-badge">
          <span>{{ section.settings.contact_text | default: 'Contact Us' | escape }}</span>
          <div class="contact-arrow-icon">
            <svg
              viewBox="0 0 10.417 10.3762"
              xmlns="http://www.w3.org/2000/svg"
              xmlns:xlink="http://www.w3.org/1999/xlink"
              width="10.416992"
              height="10.376221"
              fill="none"
              customFrame="#000000">
              <g id="组合 24">
              <g id="组合 3">
                <path
                  id="多边形 7"
                  d="M-0.353553 3.26282L3.61638 -0.707107L7.58631 3.26282L6.8792 3.96993L3.26282 0.353553L3.61638 0L3.96993 0.353553L0.353553 3.96993L-0.353553 3.26282Z"
                  fill="rgb(255,255,255)"
                  fill-rule="nonzero"
                  transform="matrix(0.708494,0.705717,-0.708494,0.705716,5.29272,0)" />
                <line
                  id="直线 2"
                  x1="0"
                  x2="7.23275518"
                  y1="0"
                  y2="0"
                  stroke="rgb(255,255,255)"
                  stroke-width="1"
                  transform="matrix(-0.708494,0.705716,-0.708494,-0.705717,7.73047,2.76355)" />
              </g>
            </g>
          </svg>
        </div>
      </a>
    </div>
  </div>
</nav></header><script>
function toggleMobileMenu() {
  const mobileMenu = document.getElementById('mobileMenu');
  const menuIcon = document.querySelector('.menu-icon');
  const closeIcon = document.querySelector('.close-icon');
  
  if (mobileMenu.style.display === 'none' || mobileMenu.style.display === '') {
    mobileMenu.style.display = 'block';
    menuIcon.style.display = 'none';
    closeIcon.style.display = 'block';
  } else {
    mobileMenu.style.display = 'none';
    menuIcon.style.display = 'block';
    closeIcon.style.display = 'none';
  }
}

function toggleSubmenu(submenuId) {
  const submenu = document.getElementById(submenuId);
  if (submenu.style.display === 'none' || submenu.style.display === '') {
    submenu.style.display = 'flex';
  } else {
    submenu.style.display = 'none';
  }
}

function toggleSearch() {
  // 搜索功能实现
  console.log('Search clicked');
}
</script>{% schema %}
{
  "name": "Header Navigation",
  "tag": "header",
  "class": "header-navigation",
  "settings": [
    {
      "type": "header",
      "content": "Logo Settings"
    },
    {
      "type": "image_picker",
      "id": "logo_image",
      "label": "Logo Image",
      "info": "Upload a logo image (recommended size: 64x64px). If no image is uploaded, logo text will be displayed instead."
    },
    {
      "type": "text",
      "id": "site_name",
      "label": "Site Name",
      "default": "XSPilates",
      "info": "Display name for your site (leave empty to use shop name)"
    },
    {
      "type": "text",
      "id": "logo_text",
      "label": "Logo Text",
      "default": "XS",
      "info": "Short text displayed in logo (only shown if no logo image is uploaded)"
    },
    {
      "type": "range",
      "id": "logo_size",
      "label": "Logo Size",
      "min": 32,
      "max": 80,
      "step": 4,
      "default": 48,
      "unit": "px",
      "info": "Adjust the size of your logo (both image and text logo)"
    },
    {
      "type": "header",
      "content": "Navigation Settings"
    },
    {
      "type": "link_list",
      "id": "main_menu",
      "label": "Main Menu",
      "default": "main-menu",
      "info": "Select the menu to display in the navigation"
    },
    {
      "type": "text",
      "id": "contact_text",
      "label": "Contact Button Text",
      "default": "Contact Us"
    },
    {
      "type": "text",
      "id": "contact_number",
      "label": "Contact Badge Number",
      "default": "21",
      "info": "Number displayed in contact badge"
    },
    {
      "type": "url",
      "id": "contact_link",
      "label": "Contact Button Link"
    },
    {
      "type": "header",
      "content": "Style Settings"
    },
    {
      "type": "color",
      "id": "nav_background_color",
      "label": "Navigation Background Color",
      "default": "rgba(255, 255, 255, 0.08)"
    },
    {
      "type": "color",
      "id": "nav_text_color",
      "label": "Navigation Text Color",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "contact_button_color",
      "label": "Contact Button Color",
      "default": "#DC2626"
    }
  ],
  "presets": [
    {
      "name": "Header Navigation",
      "settings": {
        "main_menu": "main-menu"
      }
    }
  ]
}{% endschema %}